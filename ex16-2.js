const number = 77.8;
const divide = 1 / 2;


console.log('type:%s %d', typeof number, number);
console.log('round', typeof number, Math.round(number));
console.log('divide:%d, %d', divide, Math.round(divide));

/*===================================================================
1. round(숫자를) 반올림하다.
2. 주어진 수를 가장 가까운 정수로 반올림하다. 
===================================================================*/